Killer Features — Ranked by Impact

📋 Team Manifests (.clim.yaml) — project-aware tool detection, clim check/clim init, multi-project management, required + optional tools

🔐 Security Scanner — CVE Alerts — check installed versions against GitHub Advisory Database. Show vulnerabilities in Dashboard + tool detail. clim check --security for CI. Enterprise unlock.

📜 SBOM Export — generate CycloneDX/SPDX from installed tools (name, version, source, license, hash). Feeds compliance pipelines. Low effort — data already exists.

🔄 Environment Profiles — named tool configurations ("work", "personal", "ml"). clim profile switch work. Like nvm but for everything.

🌐 Remote Environment Sync — clim sync ssh://server — diff local vs remote, show what's different, one-click align.

📡 Plugin System — community tool definitions. clim marketplace add user/awesome-tools. Scales catalog.

🔔 Background Update Notifications — system tray / terminal notify when tools have updates.

🧪 Tool Playground — clim try bat — temp install for current session. Try without committing.

🤖 AI Tool Discovery — "I need to process JSON" → suggests jq, gron, fx, jless. Natural language search.

📊 Tool Usage Analytics — track which tools you actually use. "You haven't used terraform in 45 days. Remove?"