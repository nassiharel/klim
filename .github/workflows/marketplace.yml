name: Marketplace

on:
  push:
    branches: [main]
    paths:
      - 'marketplace/**'
      - 'internal/marketplace/**'
  pull_request:
    paths:
      - 'marketplace/**'
      - 'internal/marketplace/**'
  schedule:
    # Run every night at 03:00 UTC to refresh GitHub metadata.
    - cron: '0 3 * * *'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    name: Validate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: go.mod
      - name: Validate marketplace files
        run: go run ./internal/marketplace/validate
      - name: Assemble smoke test
        run: go run ./internal/marketplace/assemble -fallback /dev/null -o /tmp/marketplace.yaml

  publish:
    name: Publish
    needs: validate
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: go.mod
      - name: Fetch previous marketplace.yaml as fallback
        run: |
          git fetch origin marketplace --depth=1 2>/dev/null || true
          git show origin/marketplace:marketplace.yaml > /tmp/fallback.yaml 2>/dev/null || touch /tmp/fallback.yaml
      - name: Assemble
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: go run ./internal/marketplace/assemble -fetch-github -fallback /tmp/fallback.yaml -o /tmp/marketplace.yaml
      - name: Publish to marketplace branch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          # Create a clean orphan branch, then copy in the assembled file.
          git checkout --orphan marketplace-staging
          git rm -rf --ignore-unmatch .
          git clean -fdx
          cp /tmp/marketplace.yaml marketplace.yaml
          git add marketplace.yaml
          git commit -m "Update marketplace catalog"
          git push -f origin marketplace-staging:marketplace
