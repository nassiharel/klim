name: delta
display_name: delta
category: CLI
tags:
    - git
    - diff
    - syntax-highlighting
    - pager
    - rust
binary_names:
    - delta
packages:
    winget: dandavison.delta
    choco: delta
    brew: git-delta
