---
title: "clim open"
description: Install tools from a share token
---

Install tools from a compact share token generated by `clim share`.

## Usage

```bash
clim open <token> [flags]
```

## Arguments

| Argument | Description |
|----------|-------------|
| `<token>` | Share token (starts with `clim:v1:`) |

## Flags

| Flag | Short | Description |
|------|-------|-------------|
| `--yes` | `-y` | Install all tools without prompting |

## Examples

```bash
# Interactive — confirm before installing
clim open clim:v1:H4sIAAAA...

# Non-interactive — install all
clim open clim:v1:H4sIAAAA... --yes
```

## How It Works

1. Decodes the share token
2. Resolves tools against the local catalog
3. Installs each tool via the best available package manager for the current OS

## See Also

- [`clim share`](/reference/commands/share) — Generate a share token
- [`clim import`](/reference/commands/import) — Install from a YAML manifest
